VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-publishable-key"
# Optional: if your frontend uses project id separately
VITE_SUPABASE_PROJECT_ID="your-project-ref"

# The backend Edge Functions use an AI gateway key. Set this in your Supabase
# project secrets (or in your local environment when running functions).
# Do NOT commit the real key to git.
LOVABLE_API_KEY="your-lovable-api-key-here"
